# 2026-07-04 重跑完成汇总

## 1. 网络与代理

加速器开启后，Python/GEE 没有自动走系统代理。检查发现：

- 系统代理字段：`127.0.0.1:7890`
- 实际可用 HTTP 代理端口：`127.0.0.1:7897`

本轮所有 GEE 命令均临时设置：

```powershell
$env:HTTP_PROXY='http://127.0.0.1:7897'
$env:HTTPS_PROXY='http://127.0.0.1:7897'
```

GEE 初始化成功，项目为 `dulcet-nucleus-498308-g8`。

## 2. 新湖界覆盖率已重算

输出：

```text
outputs/coverage/daily_valid_coverage_osm_boundary.csv
```

结果：

- 行数：`92`
- HLS 有影像日期：`75`
- HLS 有效覆盖率 `>= 0.15`：只有 `2024-05-14`
- HLS 有效覆盖率 `>= 0.05`：`2024-05-14`、`2024-05-17`、`2024-07-31`
- MODIS 有效覆盖率 `>= 0.15`：`12` 天
- MODIS 平均有效覆盖率：`0.06013`

与旧表相比的重要变化：

- 旧表中 HLS `>=0.15` 有 3 天；
- 新湖界后 HLS `>=0.15` 只有 1 天；
- `2024-05-17` 新湖界 HLS 覆盖率为 `0.1448`，略低于 0.15；
- `2024-07-31` 新湖界 HLS 覆盖率为 `0.0966`。

判断：之前说 HLS 3 天高覆盖，现在必须改成 **1 天高覆盖 + 2 天低覆盖候选**。

## 3. MODIS v2 已生成

输出目录：

```text
outputs/rasters/modis_daily_v2_fai1240/
```

产品波段：

1. `NDVI`
2. `FAI_1640`
3. `FAI_1240`
4. `QA`
5. `SOURCE`
6. `QUALITY`

结果：

- GeoTIFF 文件数：`92`
- CRS：`EPSG:32651`
- 分辨率：`500 m`
- nodata：`-9999`
- 有有效像元日期：`35`
- 无有效像元日期：`57`
- 总有效像元：`51,378`
- 单日最大有效像元：`7,496`，日期 `2024-07-30`

有效像元最多日期：

| date | valid_pixels | FAI_1240 mean |
|---|---:|---:|
| 20240730 | 7496 | -0.0183 |
| 20240729 | 6764 | -0.00565 |
| 20240518 | 6060 | -0.0263 |
| 20240523 | 3631 | -0.0254 |
| 20240508 | 2749 | -0.0253 |
| 20240705 | 2274 | -0.00898 |
| 20240618 | 2251 | -0.0263 |
| 20240501 | 1916 | -0.0279 |

判断：MODIS v2 已经成为当前新的主序列。相比旧 MODIS，v2 使用新湖界，并补充了文献更相关的 `FAI_1240`。

## 4. HLS 新湖界观测层已生成 3 天

输出目录：

```text
outputs/rasters/hls_observed/
```

3 个日期：

| date | valid_pixels | valid_area_km2 | NDVI mean | FAI mean |
|---|---:|---:|---:|---:|
| 20240514 | 1,516,622 | 1364.9598 | -0.3397 | -3.9909e-06 |
| 20240517 | 377,793 | 340.0137 | -0.2487 | -2.6269e-06 |
| 20240731 | 253,153 | 227.8377 | -0.2607 | -1.7503e-06 |

共同属性：

- CRS：`EPSG:32651`
- 分辨率：`30 m`
- 波段数：`5`
- nodata：`-9999`

判断：

- `2024-05-14` 是唯一较完整 HLS 高分观测日；
- `2024-05-17`、`2024-07-31` 只能作为低覆盖局部观测，不应做全湖面积判断。

## 5. 已生成的检查文件

```text
reports/osm_boundary_coverage_summary.json
reports/run_completion_summary_20260704.json
outputs/tables/modis_v2_fai1240_raster_summary.csv
reports/modis_v2_fai1240_raster_problems.json
```

## 6. 仍然存在的问题

1. MODIS v2 仍有 `57` 天无有效像元，日序列缺测仍然严重。
2. HLS 高覆盖只有 `1` 天，不能支撑每日 30 m 产品。
3. HLS FAI 数值仍在 `1e-5` 量级，不能直接套 MODIS/文献阈值。
4. OSM 湖界虽比 bbox 好，但仍不是行政权威湖界。
5. 还没重做 MODIS v2 的时间插值层；旧插值层基于旧 MODIS，应废弃或标为旧版。

## 7. 当前客观结论

本轮重跑完成了最关键的两件事：

1. 用新太湖湖界重新审计了 92 天覆盖率；
2. 生成了完整 MODIS v2 92 天产品，并补齐 HLS 3 个候选日。

项目现在可以进入下一阶段：基于 MODIS v2 重做插值层，并用 `2024-05-14` HLS 与 MODIS v2 做同日尺度一致性检查。
